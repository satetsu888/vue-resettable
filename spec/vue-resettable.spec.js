describe('vue-resettable', function () {  
    it('should have a created hook', function () {
        expect(typeof resettable.created).toBe('function')
        console.log("test running");
    })
})
